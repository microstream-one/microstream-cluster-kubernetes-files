apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "es-cluster.fullname" . }}-test-connection"
  labels:
    {{- include "es-cluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "es-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
